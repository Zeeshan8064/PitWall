# Base URL of the PitWall API, with no trailing slash.
#
# Vite inlines this at BUILD time, not runtime — it must be set when the
# bundle is built. Unset falls back to http://localhost:5000 so `npm run dev`
# works with no configuration.
VITE_API_BASE=http://localhost:5000
